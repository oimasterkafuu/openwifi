name: Build AutoHotspot

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build with MSVC
        shell: cmd
        run: |
          if not exist "build" mkdir "build"
          cl.exe /nologo /W3 /O2 /MT /Fe"build\AutoHotspot.exe" src\*.cpp /link /SUBSYSTEM:WINDOWS /ENTRY:WinMain kernel32.lib user32.lib advapi32.lib shell32.lib ucrt.lib vcruntime.lib msvcrt.lib

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: AutoHotspot
          path: build/AutoHotspot.exe

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: build/AutoHotspot.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
